# Configurações do Postgres

Para definir a senha de administrador do banco, que é gerado ao executar o container pela primeira vez:
```console
$ nano --nonewlines ./.secrets/postgres_password.txt
```